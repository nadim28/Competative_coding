#include<bits/stdc++.h>

#define fastio ios_base::sync_with_stdio(false),cin.tie(NULL)
#define endl "\n"
#define ll long long

using namespace std;

int main() {
    fastio;
    string s;
    int n;
    while()
    getline(cin,s);
    reverse(s.begin(),s.end());
    cout<<s<<endl;

    return 0;
}


